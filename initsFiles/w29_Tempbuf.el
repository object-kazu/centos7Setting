;; ruby のファイルを勝手にKillしてくれるので
;; 使用しないことにした
;;不要なバッファーを自動的に削除する
;; (require 'tempbuf)

;; ;;(add-hook 'dired-mode-hook 'turn-on-tempbuf-mode)
;; (add-hook 'custom-mode-hook 'turn-on-tempbuf-mode)
;; (add-hook 'Man-mode-hook 'turn-on-tempbuf-mode)
;; (add-hook 'view-mode-hook 'turn-on-tempbuf-mode)
;; (add-hook 'find-file-hooks 'turn-on-tempbuf-mode)
;; (add-hook 'compilation-mode-hook 'turn-on-tempbuf-mode)
;; (add-hook 'completion-list-mode-hook 'turn-on-tempbuf-mode)
;; (add-hook 'kill-buffer-hook 'turn-on-tempbuf-mode)
;; (add-hook 'electric-help-mode-hook 'turn-on-tempbuf-mode)
